version: '3.9'

services:
  python-app:
    image: python:3.12-slim
    container_name: python-runner
    stdin_open: true
    tty: true
    working_dir: /app
    volumes:
      - ./:/app
    networks:
      - pro_prj_net

networks:
  pro_prj_net:
    driver: bridge
